# Optional — leave unset for offline demos (deterministic rules ranker).
# When set, /api/discover uses Gemini by default (fail-soft to rules).
# Reviewer tools can force rules off by disabling "Use Gemini ranker".
GEMINI_API_KEY=
